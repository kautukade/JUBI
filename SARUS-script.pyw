#!pythonw.exe
# This file is rewritten by SARUS-Setup during installation with the absolute
# private-runtime interpreter path. Kept here so the PE launcher contract is visible.
raise SystemExit('Run SARUS-Setup.exe before launching SARUS.exe')
